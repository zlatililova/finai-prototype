export default {
  plugins: {
    // 1. Tailwind CSS must be loaded first to generate utility classes
    tailwindcss: {},
    // 2. Autoprefixer handles vendor prefixes for compatibility
    autoprefixer: {},
  },
};
